// Package transport implements the HTTP transport layer for the Entra mock.
//
// The folder is named "http" for clarity, but the package is not named "http" to avoid
// conflicting with the Go standard library package name.
package transport
